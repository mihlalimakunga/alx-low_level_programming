#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always zero (success)
 */

int main(void)

{
	char x;

	for (x = 'a'; x <= 'z'; a++)
	{
		if (x != 'e' && x != 'q')

			putchar(x);
	}

	putchar('\n');

	return (0);
}
